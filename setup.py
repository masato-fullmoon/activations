from setuptools import setup, find_packages
import sys
import subprocess

if __name__ == "__main__":
    setup(
            name="activations",
            version="1.0.0",
            install_requires=[]
            )
